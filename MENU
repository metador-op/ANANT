<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Restaurant Menu</title>
<style>
  body { font-family: sans-serif; margin: 0; background: #fafafa; }
  header { background: #d22; color: #fff; padding: 1rem; text-align: center; }
  .tabs { display: flex; justify-content: center; margin: 1rem 0; flex-wrap: wrap; }
  .tabs button { margin: 0.3rem; padding: 0.5rem 1rem; border: none; border-radius: 6px; background:#eee; cursor:pointer; }
  .tabs button.active { background:#fff; border:1px solid #d22; color:#d22; }
  .grid { display: grid; grid-template-columns: repeat(auto-fit,minmax(220px,1fr)); gap:1rem; padding:1rem; }
  .card { background: #fff; border-radius: 10px; padding: 1rem; box-shadow: 0 2px 6px rgba(0,0,0,0.1);}
  .card h3 { margin: 0 0 0.5rem 0; }
  .cart-btn { position: fixed; bottom: 20px; right: 20px; background: #d22; color: #fff;
              border:none; border-radius:50%; width:60px; height:60px; font-size:20px;
              display:flex; align-items:center; justify-content:center; cursor:pointer; }
  .drawer { position: fixed; top:0; right:-320px; width:300px; height:100%;
            background:#fff; box-shadow:-2px 0 6px rgba(0,0,0,0.2); padding:1rem;
            transition: right 0.3s ease; overflow-y:auto; }
  .drawer.open { right:0; }
  .drawer h2 { margin-top:0; }
  .drawer button { margin-top:1rem; padding:0.5rem 1rem; background:#d22; color:#fff; border:none; cursor:pointer; }
</style>
</head>
<body>

<header>
  <h1>Our Menu</h1>
</header>

<div class="tabs" id="categoryTabs"></div>
<div class="grid" id="menuGrid"></div>

<button class="cart-btn" id="cartButton">🛒<span id="cartCount">0</span></button>

<div class="drawer" id="cartDrawer">
  <h2>Your Cart</h2>
  <ul id="cartList"></ul>
  <p><strong>Total:</strong> ₹<span id="cartTotal">0</span></p>
  <button id="closeCart">Close</button>
</div>

<script>
const categories = [
  { key:"appetizers", label:"Appetizers" },
  { key:"mains",      label:"Main Courses" },
  { key:"desserts",   label:"Desserts" },
  { key:"drinks",     label:"Beverages" }
];

const menuItems = [
  { id:1, name:"Bruschetta", category:"appetizers", price:120, available:true},
  { id:2, name:"tikka", category:"appetizers", price:220, available:true},
  { id:3, name:"Veg Burger", category:"mains", price:200, available:true },
  { id:4, name:"Chocolate Cake", category:"desserts", price:150, available:true },
  { id:5, name:"Iced Coffee", category:"drinks", price:90, available:true },
];

let activeCategory = categories[0].key;
let cart = [];

function renderCategories() {
  const tabs = document.getElementById("categoryTabs");
  tabs.innerHTML = "";
  categories.forEach(cat => {
    const btn = document.createElement("button");
    btn.textContent = cat.label;
    btn.className = cat.key === activeCategory ? "active" : "";
    btn.onclick = () => { activeCategory = cat.key; renderMenu(); renderCategories(); };
    tabs.appendChild(btn);
  });
}

function renderMenu() {
  const grid = document.getElementById("menuGrid");
  grid.innerHTML = "";
  menuItems
    .filter(item => item.category === activeCategory && item.available)
    .forEach(item => {
      const card = document.createElement("div");
      card.className = "card";
      card.innerHTML = `<h3>${item.name}</h3><p>₹${item.price}</p>`;
      const addBtn = document.createElement("button");
      addBtn.textContent = "Add to Cart";
      addBtn.onclick = () => addToCart(item);
      card.appendChild(addBtn);
      grid.appendChild(card);
    });
}

function addToCart(item) {
  const existing = cart.find(c => c.id === item.id);
  if (existing) existing.qty += 1;
  else cart.push({ ...item, qty: 1 });
  updateCartUI();
}

function updateCartUI() {
  document.getElementById("cartCount").textContent =
    cart.reduce((s,i) => s + i.qty, 0);
  const list = document.getElementById("cartList");
  list.innerHTML = "";
  cart.forEach(item => {
    const li = document.createElement("li");
    li.textContent = `${item.name} x${item.qty} - ₹${item.price * item.qty}`;
    list.appendChild(li);
  });
  const total = cart.reduce((s,i) => s + i.qty * i.price, 0);
  document.getElementById("cartTotal").textContent = total;
}

document.getElementById("cartButton").onclick = () =>
  document.getElementById("cartDrawer").classList.add("open");
document.getElementById("closeCart").onclick = () =>
  document.getElementById("cartDrawer").classList.remove("open");

renderCategories();
renderMenu();
</script>
</body>
</html>

convert this code in css
